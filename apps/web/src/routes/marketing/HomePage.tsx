/** Marketing landing page (`/`). Eagerly loaded so the initial route paints without a chunk fetch. */
export default function HomePage() {
  return (
    <>
      <h1>Studafy</h1>
      <p>Welcome to Studafy. This is the marketing home page.</p>
    </>
  );
}
