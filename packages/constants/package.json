{
  "name": "@studafy/constants",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist && tsc --project tsconfig.build.json --emitDeclarationOnly --declaration --outDir dist --noEmit false",
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "test": "bun test",
    "docs:generate": "bun run scripts/generate-permission-matrix.ts && prettier --write docs/permission-matrix.md"
  },
  "devDependencies": {
    "@studafy/config": "workspace:*",
    "@studafy/tsconfig": "workspace:*",
    "@types/bun": "1.3.14",
    "typescript": "^5.9.3"
  }
}
